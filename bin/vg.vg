module bin

import io
import compiler
import vm
import sys

function main():
    args = sys.argv
    if len(args) < 2:
        print "Usage: vg <file>"
        return
    path = args[1]
    code = compiler.parse_file(path)
    vm.execute_bytecode(code)
